@model IEnumerable<apotek.Models.Pembeli>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UsernamePembeli)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PasswordPembeli)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UsernamePembeli)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PasswordPembeli)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPembeli">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPembeli">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPembeli">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
